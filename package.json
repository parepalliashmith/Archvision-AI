{
  "name": "archvision-ai",
  "version": "1.0.0",
  "description": "AI-powered house design studio: upload a 2D floor plan or describe your dream home, get an AI-generated layout and an interactive 3D visualization.",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env server.js",
    "build": "npm install --prefix client && npm run build --prefix client",
    "postinstall": "npm install --prefix client"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "pg": "^8.23.0"
  }
}
